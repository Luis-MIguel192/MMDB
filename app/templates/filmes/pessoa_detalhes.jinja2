{% extends "_layout.jinja2" %}

{% block content %}
<div class="container mt-4">
    <div class="row">
        <div class="col-12">
            <nav aria-label="breadcrumb">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a href="{{ url_for('root.index') }}">Início</a></li>
                    <li class="breadcrumb-item"><a href="{{ url_for('filmes.listar_pessoas') }}">Pessoas</a></li>
                    <li class="breadcrumb-item active" aria-current="page">{{ pessoa.nome }}</li>
                </ol>
            </nav>
            
            <div class="row">
                <div class="col-md-4">
                    {% if pessoa.foto_url %}
                        <img src="{{ pessoa.foto_url }}" class="img-fluid rounded shadow" alt="Foto de {{ pessoa.nome }}" 
                             style="max-height: 500px; width: 100%; object-fit: cover;">
                    {% else %}
                        <div class="bg-light rounded d-flex align-items-center justify-content-center flex-column" style="height: 300px;">
                            <svg class="bi mb-3" width="4em" height="4em" fill="currentColor">
                                <use xlink:href="/bootstrap/static/icons/bootstrap-icons.svg#person-circle"/>
                            </svg>
                            <span class="text-muted">Sem foto disponível</span>
                        </div>
                    {% endif %}
                </div>
                <div class="col-md-8">
                    <h1>👤 {{ pessoa.nome }}</h1>
                    {% if pessoa.nome_completo and pessoa.nome_completo != pessoa.nome %}
                        <h2 class="h4 text-muted mb-3">{{ pessoa.nome_completo }}</h2>
                    {% endif %}
                    
                    <div class="mb-3">
                        {% if pessoa.nacionalidade %}
                            <span class="badge bg-primary me-2">{{ pessoa.nacionalidade }}</span>
                        {% endif %}
                        {% if pessoa.data_nascimento %}
                            <span class="badge bg-secondary">{{ pessoa.data_nascimento.strftime('%d/%m/%Y') }}</span>
                        {% endif %}
                    </div>
                    
                    {% if pessoa.biografia %}
                        <div class="mb-4">
                            <h3>Biografia</h3>
                            <p class="lead">{{ pessoa.biografia }}</p>
                        </div>
                    {% endif %}
                </div>
            </div>
            
            {% if participacoes %}
                <div class="row mt-5">
                    <div class="col-12">
                        <h3>🎬 Filmografia</h3>
                        <div class="row">
                            {% for participacao in participacoes %}
                            <div class="col-md-6 col-lg-4 mb-3">
                                <div class="card h-100">
                                    {% if participacao.filme.poster_url %}
                                        <img src="{{ participacao.filme.poster_url }}" class="card-img-top" alt="Poster de {{ participacao.filme.titulo }}" 
                                             style="height: 250px; object-fit: cover;">
                                    {% endif %}
                                    <div class="card-body">
                                        <h5 class="card-title">
                                            <a href="{{ url_for('filmes.detalhes_filme', filme_id=participacao.filme.id) }}" class="text-decoration-none">
                                                {{ participacao.filme.titulo }}
                                            </a>
                                        </h5>
                                        <p class="card-text">
                                            <strong class="job-role" 
                                                    data-bs-toggle="tooltip" 
                                                    data-bs-placement="top" 
                                                    data-cargo="{{ participacao.cargo }}"
                                                    style="cursor: help; border-bottom: 1px dotted #74b9ff; color: #74b9ff;"
                                                    title="">{{ participacao.cargo }}</strong>
                                            {% if participacao.personagem %}
                                                <br><small class="text-muted">como {{ participacao.personagem }}</small>
                                            {% endif %}
                                            {% if participacao.filme.ano_lancamento %}
                                                <br><small class="text-muted">{{ participacao.filme.ano_lancamento }}</small>
                                            {% endif %}
                                        </p>
                                    </div>
                                </div>
                            </div>
                            {% endfor %}
                        </div>
                    </div>
                </div>
            {% endif %}
        </div>
    </div>
</div>

{% endblock %}

{% block scripts %}
<script>
// Tooltips para cargos na filmografia da pessoa - VERSÃO DEBUG
document.addEventListener('DOMContentLoaded', function() {
    console.log('🎭 [DEBUG] Inicializando tooltips na página de pessoa...');
    console.log('🎭 [DEBUG] Bootstrap disponível:', typeof window.bootstrap !== 'undefined');
    console.log('🎭 [DEBUG] Bootstrap.Tooltip disponível:', window.bootstrap && typeof bootstrap.Tooltip !== 'undefined');
    
    // Descrições dos cargos
    const cargos = {
        'Diretor': 'Responsável pela visão criativa geral do filme, dirigindo atores e coordenando a equipe técnica.',
        'Ator': 'Interpreta um personagem no filme, trazendo vida às palavras do roteiro através da performance.',
        'Atriz': 'Interpreta um personagem no filme, trazendo vida às palavras do roteiro através da performance.',
        'Produtor': 'Gerencia os aspectos financeiros e logísticos da produção, desde o desenvolvimento até a distribuição.',
        'Roteirista': 'Escreve o roteiro do filme, criando diálogos, estrutura narrativa e desenvolvimento de personagens.',
        'Editor': 'Monta o filme final, selecionando e organizando as cenas para criar o ritmo e fluxo narrativo.',
        'Compositor': 'Cria a trilha sonora original do filme, compondo músicas que complementam a narrativa.',
        'Produtor Executivo': 'Supervisiona múltiplos projetos e fornece financiamento ou recursos para a produção.'
    };

    // Aguardar um pouco para garantir que o DOM está pronto
    setTimeout(function() {
        console.log('🎭 [DEBUG] Procurando elementos .job-role...');
        const jobRoles = document.querySelectorAll('.job-role');
        console.log('🎭 [DEBUG] Encontrados', jobRoles.length, 'elementos com classe job-role');
        
        // Debug: listar todos os elementos strong na página
        const allStrongs = document.querySelectorAll('strong');
        console.log('🎭 [DEBUG] Total de elementos strong na página:', allStrongs.length);
        allStrongs.forEach((strong, i) => {
            console.log(`🎭 [DEBUG] Strong ${i + 1}:`, strong.textContent.trim(), 'Classes:', strong.className);
        });
        
        jobRoles.forEach(function(element, index) {
            const cargo = element.getAttribute('data-cargo') || element.textContent.trim();
            const descricao = cargos[cargo];
            
            console.log(`🎭 [DEBUG] Processando elemento ${index + 1}:`, cargo);
            
            if (descricao) {
                console.log(`✅ [DEBUG] Configurando tooltip ${index + 1} para cargo:`, cargo);
                
                // Definir o título do tooltip
                element.setAttribute('title', descricao);
                
                // Adicionar evento de hover manual para debug
                element.addEventListener('mouseenter', function() {
                    console.log('🖱️ [DEBUG] Mouse entrou em:', cargo);
                });
                
                element.addEventListener('mouseleave', function() {
                    console.log('🖱️ [DEBUG] Mouse saiu de:', cargo);
                });
                
                // Inicializar tooltip Bootstrap
                if (window.bootstrap && bootstrap.Tooltip) {
                    try {
                        new bootstrap.Tooltip(element, {
                            placement: 'top',
                            trigger: 'hover focus'
                        });
                        console.log(`🎯 [DEBUG] Tooltip Bootstrap criado para:`, cargo);
                    } catch (error) {
                        console.error('❌ [DEBUG] Erro ao criar tooltip:', error);
                    }
                } else {
                    console.log('❌ [DEBUG] Bootstrap.Tooltip não disponível');
                }
            } else {
                console.log('❌ [DEBUG] Cargo não encontrado:', cargo);
            }
        });
        
        console.log('🎉 [DEBUG] Inicialização de tooltips concluída!');
    }, 1000); // Aumentei o timeout para 1 segundo
});
</script>
{% endblock %}